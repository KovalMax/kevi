[package]
name = "kevi-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0.203", features = ["derive"] }
totp-rs = { version = "5.5", default-features = true, features = ["otpauth"] }
secrecy = { version = "0.10.3", features = ["serde"] }
